# proyecto-react-native

**Ejecutar App**
```
npm run web
```

**Reinicia el servidor con caché limpio:**

```
npm start -- --reset-cache
```

```
rm -rf node_modules
rm package-lock.json
rm -rf .expo
npm install
npm start -- --reset-cache
```

npx expo start -c
